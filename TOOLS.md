# TOOLS.md - Minecraft Bot Tools

You have exactly 7 tools. Use ONLY these exact tool names in your plan:

1. `mine` — args: `{ "block_type": "oak_log" }`
2. `craft` — args: `{ "item_name": "wooden_pickaxe", "count": 1 }`
3. `move_to` — args: `{ "x": 100, "y": 64, "z": 200 }`
4. `equip` — args: `{ "item_name": "stone_sword" }`
5. `attack` — args: `{ "target_name": "zombie" }`
6. `eat` — args: `{ "food_name": "beef" }` or `{}`
7. `chat` — args: `{ "message": "hello" }`

No other tools exist. No minecraft: prefix. No exec/write/read/search.
